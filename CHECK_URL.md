# 页面与链接核对

[返回首页](./README.md) · [静态部署](./DEPLOYMENT.md)

下表列出源码中已有的页面入口。开发时使用终端地址；静态预览时以 `http://127.0.0.1:8080` 为根地址。这里只列应检查的项目，不预先勾选未执行的验收。

| 路径 | 页面 | 核对重点 |
| --- | --- | --- |
| `/` | 首页 | 配置资料、主导航与作品链接 |
| `/about/` | 关于 | 个人介绍与时间信息准确性 |
| `/blog/` | 文章列表 | 发布过滤、日期排序、卡片与正文入口 |
| `/projects/` | 项目展示 | 仓库链接、演示地址与隐藏条目 |
| `/skills/` | 技能展示 | 分类、顺序与手工统计的含义 |
| `/contact/` | 联系 | 链接、表单验证及真实提交目标 |

## 必须区分的行为

页面出现“分类”按钮，不等于按钮已绑定筛选；出现联系表单，不等于邮件已经发送。`lib/content.ts` 返回文章 `body` 和 `slug`，也不等于已经实现 `/blog/<slug>/` 详情路由。先按源码确认实现，再记录实际验收结果。

## 核对方法

从首页点击进入每个页面，然后直接输入该页面地址并刷新。对图片检查实际加载结果，不只看 HTML 存在 `<img>`。对外部项目地址确认仓库所有者和当前名称，不要用短标题猜测新地址。

新增页面后在这里补一行。报告问题时记录：提交号、运行模式（开发或静态预览）、访问路径、复现步骤、预期/实际结果，以及脱敏的浏览器控制台信息。不要提交登录信息、令牌或真实用户数据。
