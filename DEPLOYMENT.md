# 静态构建与部署

[返回首页](./README.md) · [内容维护](./CMS_GUIDE.md) · [页面核对](./CHECK_URL.md)

## 当前配置

[next.config.js](./next.config.js) 设置了 `output: 'export'`、`images.unoptimized: true` 和 `trailingSlash: true`。本仓库的发布对象是静态文件，不是一个需要 `next start` 托管的 Node.js 服务。

## 构建

```sh
npm install
npm run build
```

构建成功后检查 `out/` 中的实际内容。构建失败时先修复报错，不上传旧目录冒充最新版本。依赖安装可能执行依赖包脚本，请在受控开发环境中进行。

## 本地预览产物

在已经安装 Python 3 的机器上，可从仓库根目录运行：

```sh
python -m http.server 8080 --bind 127.0.0.1 --directory out
```

访问 `http://127.0.0.1:8080/`，逐项验证[页面清单](./CHECK_URL.md)。这只是本机静态预览，不会部署到任何云服务；没有 Python 时使用已有的静态文件服务器即可。

## 托管配置

| 项目 | 设置 |
| --- | --- |
| 构建工作目录 | 仓库根目录 |
| 构建命令 | `npm run build` |
| 输出目录 | `out` |
| 运行时 | 静态托管，无需 Node.js 常驻进程 |
| 路径行为 | 保持目录索引和尾斜杠路径可用 |

上传 `out/` 内容到自己管理的静态托管环境。首次发布先用预览地址测试，不在未检查的情况下替换生产站点。若部署在域名的子路径而非根路径，需要单独核对 Next.js 路径配置和静态资源地址。

不要将所有未知请求盲目重写到首页来掩盖缺失页面；文章列表、文章详情和 404 应按实际导出的页面区分处理。

## 发布核对

确认首页和直接访问的子页面都能加载，刷新不报错；检查图片、站内链接、外部链接、移动端与控制台错误。联系表单需要独立后端或表单服务，静态托管本身不会发送邮件。

发布前保存前一版静态产物和对应提交号，回滚时恢复整版内容，不混用不同构建的页面与资源。此文档没有配置域名、DNS、部署平台或生产凭据，也不声明当前站点在线。

参考：[Next.js 14 静态导出文档](https://nextjs.org/docs/14/app/building-your-application/deploying/static-exports)。
